var username = "kaYes";
username = username.toLowerCase()
var final;
final = username.slice(0,1).toUpperCase();
final =final + username.slice(1,username.length);

console.log(final) /* printing statements */
